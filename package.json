{
  "name": "buntest",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun --env-file=./.env run index.ts",
    "start:watch": "bun --watch run index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
